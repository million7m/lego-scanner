node_modules/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
coverage/

# local experiments / scratch — not part of the app
files.zip
genkey.bat
regenerate_ssh_key.ps1
root.html
tmp_*

# harvester working files. data/sets.json, data/barcodes.json and
# data/contributed.json ARE committed — they're what the app serves.
data/cache/
data/harvest-state.json
